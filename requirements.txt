# ==========================================
# IAGRO - Dependências do Backend (Python)
# ==========================================

# --- Core da API (Velocidade e Roteamento) ---
fastapi==0.104.1         # Framework web super rápido para criar a API
uvicorn[standard]==0.24.0 # Servidor para rodar o FastAPI
pydantic==2.5.2          # Validação de dados (ex: validar números de CAR)
python-dotenv==1.0.0     # Para gerenciar variáveis de ambiente e senhas de forma segura

# --- Inteligência Artificial & Machine Learning ---
scikit-learn==1.3.2      # Modelos de regressão para cálculo de depreciação
pandas==2.1.3            # Manipulação de grandes volumes de dados
xgboost==2.0.2           # Algoritmo preditivo de alta performance para precificação de Safra
numpy==1.26.2            # Cálculos matemáticos complexos de Valuation

# --- Land Intelligence (Geoprocessamento) ---
geopandas==0.14.1        # Essencial para analisar dados de bioma e solo do MATOPIBA
Shapely==2.0.2           # Manipulação geométrica de polígonos de fazendas

# --- Banco de Dados e Integração ---
SQLAlchemy==2.0.23       # ORM para conversar com o banco de dados
psycopg2-binary==2.9.9   # Driver para conectar com o PostgreSQL (PostGIS)
